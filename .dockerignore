# Whitelist: exclude everything, then re-include only what the Dockerfile COPYs.
# Keeps the torch training model, venvs, and training data out of the build context.
*
!requirements-production.txt
!server.py
!inference_onnx.py
!text_norm.py
!aisle_map.py
!aisle_model_onnx/
!NOTICE
!LICENSE
