# Serving deps only - torch-free. The model runs as int8 ONNX (see inference_onnx.py).
# No torch / transformers / sentence-transformers / pandas / scikit-learn needed.
onnxruntime>=1.20.0
tokenizers>=0.20.0
numpy>=1.24.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
